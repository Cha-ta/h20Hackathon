---
phase: 01-foundation-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - app/layout.tsx
  - app/globals.css
  - vitest.config.ts
  - src/test/setup.ts
autonomous: true
requirements:
  - TECH-01
  - TECH-02
user_setup: []
must_haves:
  truths:
    - "The repository contains a working Next.js App Router application."
    - "TypeScript is configured and can be checked through a dedicated script."
    - "A runnable automated test harness exists for later phase verification."
  artifacts:
    - path: "package.json"
      provides: "Next.js project scripts and dependencies"
      contains: "\"next\""
    - path: "app/layout.tsx"
      provides: "Root App Router layout"
      exports: ["default"]
    - path: "vitest.config.ts"
      provides: "Vitest configuration for the project"
      min_lines: 10
    - path: "src/test/setup.ts"
      provides: "Shared test setup"
      min_lines: 3
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test script"
      pattern: "vitest"
    - from: "package.json"
      to: "app/layout.tsx"
      via: "Next.js build pipeline"
      pattern: "\"build\""
---

<objective>
Create the project scaffold and verification tooling for the weather app.

Purpose: Establish the Next.js + TypeScript baseline so later work can focus on data and UI instead of setup.
Output: Working App Router scaffold, scripts, and a minimal validation harness.
</objective>

<execution_context>
@/Users/danielvillasenor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danielvillasenor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Next.js App Router project</name>
  <files>package.json, tsconfig.json, next.config.ts, eslint.config.mjs, app/layout.tsx, app/globals.css</files>
  <action>Create a clean Next.js + TypeScript App Router baseline in the current repo. Include the standard scripts needed for development, linting, building, and type-checking. Keep the structure intentionally small and aligned to a single-page experience rather than generating unnecessary feature folders. Do not implement timeline logic here.</action>
  <verify>npm run lint</verify>
  <done>The repository builds as a Next.js project and has dedicated scripts for linting and type-checking.</done>
</task>

<task type="auto">
  <name>Task 2: Install and wire the validation harness</name>
  <files>package.json, vitest.config.ts, src/test/setup.ts</files>
  <action>Add a lightweight test stack using Vitest and Testing Library. Configure one-off run commands only; avoid watch-mode defaults in scripts. Make the setup reusable by later plans without requiring refactors.</action>
  <verify>npm run test -- --run</verify>
  <done>Vitest configuration exists, the shared test setup loads correctly, and the test script can execute in CI-style one-shot mode.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run test -- --run` executes without configuration errors
</verification>

<success_criteria>

- All tasks completed
- The repository now behaves like a valid Next.js + TypeScript app
- Test infrastructure exists and can be reused by later plans
- No timeline or weather logic is hardcoded into the scaffold

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-01-SUMMARY.md`
</output>
