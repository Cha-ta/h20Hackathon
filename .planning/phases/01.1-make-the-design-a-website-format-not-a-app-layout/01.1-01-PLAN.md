---
phase: 01.1-make-the-design-a-website-format-not-a-app-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - src/components/weather-app-shell.tsx
  - src/components/weather-scene-shell.tsx
autonomous: true
requirements:
  - EXP-01
user_setup: []
must_haves:
  truths:
    - "The main page reads as a website layout rather than a mobile app card."
    - "The scene area remains prominent within the new website composition."
    - "The January 2016 default weather state is still displayed through the same data-driven shell."
  artifacts:
    - path: "app/globals.css"
      provides: "Website-scale layout and framing styles"
      min_lines: 40
    - path: "src/components/weather-app-shell.tsx"
      provides: "Website-format shell structure"
      min_lines: 40
    - path: "src/components/weather-scene-shell.tsx"
      provides: "Scene area still integrated into the new page layout"
      min_lines: 15
  key_links:
    - from: "src/components/weather-app-shell.tsx"
      to: "src/components/weather-scene-shell.tsx"
      via: "scene composition inside website layout"
      pattern: "WeatherSceneShell"
---

<objective>
Refactor the weather experience shell from an app-like frame into a website-format layout.

Purpose: Correct the product presentation before timeline and interaction work continue.
Output: Website-scale page structure, updated layout styles, and preserved scene prominence.
</objective>

<execution_context>
@/Users/danielvillasenor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danielvillasenor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-03-SUMMARY.md
@.planning/phases/01.1-make-the-design-a-website-format-not-a-app-layout/01.1-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace the mobile-card frame with a website shell</name>
  <files>src/components/weather-app-shell.tsx, app/globals.css</files>
  <action>Refactor the page structure so it reads as a website rather than a centered phone mockup. Use a real page wrapper, content-width layout, and distinct hero/scene/support regions. Preserve the existing data-driven date and metrics content while changing the page architecture around it.</action>
  <verify>npm run test -- --run src/app/page.test.tsx</verify>
  <done>The page no longer relies on a mobile-device card frame and instead renders as a website composition.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the scene as the visual anchor inside the website layout</name>
  <files>src/components/weather-app-shell.tsx, src/components/weather-scene-shell.tsx, app/globals.css</files>
  <action>Rebalance the scene region so it remains visually dominant inside the new website layout. Keep the scene shell reusable and avoid collapsing it into a small secondary element. The correction should make later 3D work easier, not force another layout rewrite.</action>
  <verify>npm run build</verify>
  <done>The scene remains a primary page element inside a website-format layout and the app still builds successfully.</done>
</task>
</tasks>

<verification>
- [ ] `npm run test -- --run src/app/page.test.tsx` passes
- [ ] `npm run build` passes
- [ ] The page structure reads as a website shell instead of a mobile app frame
</verification>

<success_criteria>
- All tasks completed
- Website layout correction is visible in the shell
- Default record rendering still works
- Scene region remains ready for later 3D upgrades
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-make-the-design-a-website-format-not-a-app-layout/01.1-01-SUMMARY.md`
</output>
