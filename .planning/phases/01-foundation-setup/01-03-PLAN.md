---
phase: 01-foundation-setup
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - app/page.tsx
  - src/components/weather-app-shell.tsx
  - src/components/weather-scene-shell.tsx
  - src/app/page.test.tsx
autonomous: true
requirements:
  - TECH-03
  - TIME-03
user_setup: []
must_haves:
  truths:
    - "The home page renders from a React component structure that can support later timeline and 3D work."
    - "The initial page state is wired to the normalized earliest weather record."
    - "The scene area is framed and componentized so later 3D work can plug in without replacing the page shell."
  artifacts:
    - path: "app/page.tsx"
      provides: "Initial page entry point using the normalized default record"
      exports: ["default"]
    - path: "src/components/weather-app-shell.tsx"
      provides: "Top-level weather experience shell"
      min_lines: 25
    - path: "src/components/weather-scene-shell.tsx"
      provides: "Scene boundary for later React-based 3D work"
      min_lines: 20
    - path: "src/app/page.test.tsx"
      provides: "Default-state render verification"
      min_lines: 15
  key_links:
    - from: "app/page.tsx"
      to: "src/lib/weather-data.ts"
      via: "default-record load"
      pattern: "weather-data|get"
    - from: "src/components/weather-app-shell.tsx"
      to: "src/components/weather-scene-shell.tsx"
      via: "scene composition"
      pattern: "weather-scene-shell|WeatherSceneShell"
---

<objective>
Wire the normalized default weather state into the initial app shell.

Purpose: Finish the phase with a React-based page shell that already respects the locked composition direction and renders the January 1, 2016 default state through reusable components.
Output: Home page entry point, app shell, scene shell, and a render test for the default state.
</objective>

<execution_context>
@/Users/danielvillasenor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danielvillasenor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
@.planning/phases/01-foundation-setup/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the initial weather app shell</name>
  <files>app/page.tsx, src/components/weather-app-shell.tsx</files>
  <action>Use the normalized weather-data utility to load the earliest record on the home page and pass it into a dedicated React shell component. The shell should establish the core page zones needed later: hero/date area, scene area, and space for timeline controls. Keep it intentionally foundational rather than feature-complete.</action>
  <verify>npm run test -- --run src/app/page.test.tsx</verify>
  <done>The home page renders from reusable React components and is driven by the normalized default weather record.</done>
</task>

<task type="auto">
  <name>Task 2: Establish the scene boundary and framing shell</name>
  <files>src/components/weather-scene-shell.tsx, src/components/weather-app-shell.tsx</files>
  <action>Create a dedicated scene shell component that reserves the locked composition and future 3D entry point. It should support near-final framing intentions and ambient-scene readiness without fully implementing all weather effects yet. Avoid tying the shell to a specific 3D library runtime in this phase if that would inflate setup complexity.</action>
  <verify>npm run build</verify>
  <done>The page includes a reusable scene boundary that later phases can upgrade into the final 3D experience without replacing the overall shell.</done>
</task>

<task type="auto">
  <name>Task 3: Verify default-state rendering</name>
  <files>src/app/page.test.tsx</files>
  <action>Add a render test that proves the page shell receives and shows the expected default January 1, 2016 state. Keep the assertion focused on foundational behavior rather than final styling details.</action>
  <verify>npm run test -- --run src/app/page.test.tsx</verify>
  <done>The default home page state is verified by automation and tied to the earliest normalized record.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] `npm run test -- --run src/app/page.test.tsx` passes
- [ ] The page shell is driven by the normalized earliest record, not hardcoded mock state
</verification>

<success_criteria>

- All tasks completed
- A reusable React page shell exists for the weather experience
- The initial state comes from January 1, 2016 data
- The scene area is prepared for later 3D upgrades without app-shell rewrites

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-03-SUMMARY.md`
</output>
