---
phase: 01-foundation-setup
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/weather-data.ts
  - src/lib/weather-types.ts
  - src/lib/weather-state.ts
  - src/lib/weather-data.test.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - TIME-03
  - TECH-02
user_setup: []
must_haves:
  truths:
    - "The app can read the provided `json.json` dataset through a reusable TypeScript utility."
    - "Weather records are normalized and sorted chronologically in ascending order."
    - "The default selected record resolves to January 1, 2016."
    - "Weather-state derivation is centralized and based on fixed rules across the dataset."
  artifacts:
    - path: "src/lib/weather-data.ts"
      provides: "Dataset loading, parsing, sorting, and default-record selection"
      min_lines: 25
    - path: "src/lib/weather-state.ts"
      provides: "Weather-state derivation rules"
      min_lines: 20
    - path: "src/lib/weather-types.ts"
      provides: "Typed weather record interfaces"
      min_lines: 10
    - path: "src/lib/weather-data.test.ts"
      provides: "Automated verification for parsing and default-date behavior"
      min_lines: 20
  key_links:
    - from: "src/lib/weather-data.ts"
      to: "json.json"
      via: "dataset read"
      pattern: "json\\.json"
    - from: "src/lib/weather-data.ts"
      to: "src/lib/weather-state.ts"
      via: "state derivation during normalization"
      pattern: "weather-state|derive"
---

<objective>
Create the typed weather-data domain layer for the app.

Purpose: Ensure the product has one trusted source of truth for parsing the dataset, sorting records, deriving weather states, and selecting the initial January 1, 2016 state.
Output: Typed weather utilities, rule-based weather mapping helpers, and unit tests.
</objective>

<execution_context>
@/Users/danielvillasenor/.codex/get-shit-done/workflows/execute-plan.md
@/Users/danielvillasenor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the weather domain model</name>
  <files>src/lib/weather-types.ts, src/lib/weather-state.ts</files>
  <action>Create explicit TypeScript types for raw records, normalized records, and weather-state labels. Implement a dedicated weather-state derivation helper that uses the locked fixed-threshold rules from CONTEXT.md and keeps the expressive state naming centralized. Avoid leaking raw parsing logic into component code.</action>
  <verify>npm run typecheck</verify>
  <done>Typed weather models and weather-state derivation utilities exist and reflect the locked phase decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dataset normalization pipeline</name>
  <files>src/lib/weather-data.ts</files>
  <action>Create a reusable utility that reads the provided `json.json`, parses each date, sorts all records ascending, derives the weather state, and exposes helpers for retrieving the default earliest record. Preserve the raw file as the source of truth rather than duplicating the dataset into a second canonical file.</action>
  <verify>npm run test -- --run src/lib/weather-data.test.ts</verify>
  <done>The app has a single reusable utility for loading the dataset and producing a normalized timeline whose first record is January 1, 2016.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated verification for the weather pipeline</name>
  <files>src/lib/weather-data.test.ts</files>
  <action>Add focused tests for date parsing, chronological sorting, default-record selection, and at least representative weather-state derivation cases. Keep the tests tied to the fixed-threshold business rules rather than brittle snapshot output.</action>
  <verify>npm run test -- --run src/lib/weather-data.test.ts</verify>
  <done>Core data behavior is covered by automated tests and can catch regressions before UI work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck` passes
- [ ] `npm run test -- --run src/lib/weather-data.test.ts` passes
- [ ] Test assertions include default-date verification for January 1, 2016
</verification>

<success_criteria>

- All tasks completed
- Weather data access is centralized and typed
- The dataset is demonstrably sorted ascending
- The earliest default record is verified as January 1, 2016
- Weather mapping rules are explicit and reusable

</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-02-SUMMARY.md`
</output>
